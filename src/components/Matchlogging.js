// MatchLogging.js
const MatchLogging = ({ user }) => {
    return (
        <div>
          <h2>Match Logging for {user?.name}</h2>
          {/* Match logging form and functionality */}
        </div>
      );
}

export default MatchLogging;