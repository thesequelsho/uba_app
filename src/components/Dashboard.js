// Note: Shows user stats, rank, and recent matches
const Dashboard = ({ user }) => {
  return (
    <div className="space-y-6">
      <h1>Welcome To The UBA {user?.name}</h1>
      {/* Stats cards and charts */}
    </div>
  );
};

export default Dashboard;